package appinfo

import (
	"time"
)

var (
	Name = "mockserver"
	Title = "Mock Server"
	Description =  "High-performance mock API server ready."

	// Application version
	Version = "1.0.0"

	StartTime = time.Now()
)
